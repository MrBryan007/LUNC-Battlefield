/* Unit builders / formations. Core implementations live in battle-engine.js for v7 Phase 1; this module reserves the API surface. */
(function (global) {
  'use strict';
  global.LUNCBattle = global.LUNCBattle || {};
  global.LUNCBattle.units = global.LUNCBattle.units || {
    version: 'v7-phase1',
    note: 'Infantry/armor/artillery builders currently owned by battle-engine.js; further extraction in a later commit.'
  };
})(window);
